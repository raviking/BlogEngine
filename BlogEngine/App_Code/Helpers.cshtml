
@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper,List<BlogEngine.DTO.Tag> tags)
    {
        foreach (var tag in tags)
        {
            <div class="tags-div">                
                @BlogEngine.ActionLinkExtensions.TagLink(htmlHelper,tag)
            </div>
        }
    }

